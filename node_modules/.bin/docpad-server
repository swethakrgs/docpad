#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../docpad/bin/docpad-server" "$@"
  ret=$?
else 
  node  "$basedir/../docpad/bin/docpad-server" "$@"
  ret=$?
fi
exit $ret
